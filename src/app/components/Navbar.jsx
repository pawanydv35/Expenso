'use client'

import Image from "next/image";

export default function Home() {
  return (
   <nav>
    <div className="menu ">
        <a href="#">Home</a>
        <a href="#">Dashboard</a>
        <a href="#">Features</a>
        <a href="#">About us</a>
       
    </div>
   </nav>
  );
}
